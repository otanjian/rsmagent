## 1. 基线与依赖切片核验

- [x] 1.1 核对 `chat.html` 实际脚本、样式和模板加载链，记录在用侧栏、创建团队、邀请成员、@ 候选与历史入口的位置，避免修改未加载副本。（证据 1.1）
- [x] 1.2 核实 `add-opencode-coding-agents` 的真实类型投影、coding 普通路径拒绝及编码历史实现；记录可运行测试和结果，不以任务已勾选替代证据。（证据 1.2）
- [x] 1.3 核实统一默认解析、当前身份使用范围、会话 settings 授权与持久化、归档/重命名及离页保护的现有合同，记录与在途图片输入、任务页面 change 的共享文件边界。（证据 1.3）
- [x] 1.4 建立本 change 的 evidence 文档与验收矩阵，将阶段 1 的真实检查结果列为进入阶段 2 的前置，不修改其他 change 或运行数据。（证据 1.4 / 验收矩阵）

## 2. 团队候选与服务端边界

- [x] 2.1 确认阶段 1 的类型、授权与作用域证据通过；实现独立的普通团队候选投影，真实旧档案缺类型兼容 normal，未知对象和加载失败不回退为可选。（证据 2.1–2.2）
- [x] 2.2 将创建弹窗、搜索、预选、默认响应者、已选摘要、邀请成员和团队 @ 候选全部接入同一过滤规则，coding 不生成名称、头像、禁用行或标签；保持单人 coding 切换及目录不变。（证据 2.1–2.2）
- [x] 2.3 在现有团队写入路径校验 owner 与全部 members 的类型、租户、启用状态及调用者使用资格，coding/越权/未知对象整次拒绝；保留成员去重和清空后回到单人语义。（证据 2.3–2.5）
- [x] 2.4 使成员持久化失败向上返回明确错误，失败不更新内存名册或退休运行时；合法名册保存成功且集合变化时才调用既有重建逻辑。（证据 2.3–2.5）
- [x] 2.5 处理历史无效名册：选择界面只给通用提示，不展示 coding 身份；支持用户显式清理，保留消息，对无法解析的旧参与者容错，禁止无效对象继续运行。（证据 2.3–2.5）
- [x] 2.6 扩展 `test_coding_agent_type_boundary.py`、`test_session_team_runtime.py` 及相关路由/作用域测试，覆盖 owner/member 两种 coding 注入、撤权、跨租户、私有边界、磁盘失败和合法清理；记录真实 handler 与持久化结果后方可进入阶段 3。（证据 2.3–2.5）

## 3. 选择弹窗与可靠创建

- [x] 3.1 核对阶段 2 证据通过；实现独立弹窗草稿、名称/职责搜索、跨搜索选择、已选人数、显式默认响应者及移除默认后重新指定，至少两位不同合格对象才可提交。（证据 阶段 3）
- [x] 3.2 使侧栏与历史页团队入口共用弹窗；当前是 coding 时可打开，仅预选合格普通对象，取消保留原编码会话，候选不足时显示普通智能体空态/不足状态。（证据 阶段 3）
- [x] 3.3 实现准备会话状态与防重入，显式捕获用户/租户/认证代次、owner、preparedSessionId 和 members，settings 请求不依赖全局当前会话寻址。（证据 阶段 3）
- [x] 3.4 在确认持久化后使用同一 preparedSessionId 提交新会话视图与活动偏好，初始化默认工作空间和完整名册；此前不清空原输入、不展示成功历史、不开放新会话发送，进入时不自动调用模型。（证据 阶段 3）
- [x] 3.5 补齐 HTTP/业务/落盘失败反馈、同一尝试的超时核对与重试、取消/导航/切换身份的过期回调隔离，并确保最终提交前的未保存内容保护有效。（证据 阶段 3）
- [x] 3.6 显示团队默认响应者、成员数、合法成员和管理入口；刷新及历史恢复沿用 owner/members，普通新建的草稿/附件继承和后台流式任务行为不变。（证据 阶段 3）
- [x] 3.7 添加团队启动行为测试并回归 `test_agent_chat_launch_frontend.cjs`、`test_coding_frontend.cjs`：重点验证双击、保存失败、响应丢失、选中目标失效、coding 来源取消、离页新增编辑及租户切换；记录阶段 3 的成功与失败闭环证据。（证据 阶段 3）

## 4. 侧栏与最近会话布局

- [x] 4.1 确认阶段 3 可靠创建证据通过；接入默认关闭的临时呈现开关 `workbench_sidebar_launch_v2`，明确它只控制布局及预览数量，不控制团队类型过滤、服务端拒绝和可靠创建。（证据 4）
- [x] 4.2 在真实 Web 壳实现顶部唯一启动控件（主按钮 + 折叠箭头 + 选择菜单，见阶段 6）、功能导航既定顺序、单一中部滚动区和固定底部管理账号区；使用既有主题变量，不恢复退休菜单或增加管理权限。（证据 4 / 5.4 / 6.2–6.3）
- [x] 4.3 将最近会话预览调整为既有授权排序的前 5 条，标题区始终提供查看全部；保留折叠、已归档、并列重命名/归档、双击和 F2，完整历史路由及旧地址保持可达。（证据 4）
- [x] 4.4 从统一历史的持久化名册派生团队标识及可访问摘要，保持 coding 历史展示与打开方式；归档/恢复后按原顺序补齐前 5 条，不新增逐条名册请求。（证据 4）
- [x] 4.5 补齐三语文案、动作与导航当前态、折叠侧栏提示、手机收起遮罩与全屏弹层、键盘操作和焦点恢复；适配长名称及触摸设备。（证据 4 / 5.2：Escape 关闭与焦点恢复为本次新增实现）
- [x] 4.6 回归 `test_session_history_frontend.cjs`、侧栏归档/重命名/账号和菜单授权测试，覆盖数量上限、置顶、短列表、已归档恢复、跨身份迟到响应及控制台可见性；记录阶段 4 证据。（证据 4.6）

## 5. 集成验收与发布回退

- [x] 5.1 检查阶段 2 至 4 的真实证据后执行接口集成：混合 normal/coding 数据、仅 1 位 normal、全部 coding、旧档案缺类型、撤权、越租户及强行提交 coding；验证错误无持久化副作用，不以模拟结果代替服务端门槛。（证据 5.1）
- [x] 5.2 在实际 Web 页面逐一检查创建、搜索、默认响应、已选、邀请和团队 @ 界面的可见 DOM/辅助技术树及键盘焦点，确认 coding 对象完全不存在；验证 coding 单聊、目录和历史仍可用。（证据 5.2，真实页面 + 截图）
- [ ] 5.3 完成首条消息使用完整团队、@ 指定成员、已有委派、刷新恢复、历史重新打开及旧消息保留的端到端验收，不将仅创建成功视为运行闭环通过。（部分：名册持久化、运行时按名册重建、`@` 定址与按名册渲染已通过；**真实模型轮次未在本机环境验证**，见 evidence 末节，不以模拟结果代替）
- [x] 5.4 在桌面展开/折叠、低高度窗口、360px 手机、三语及既有三配色×明暗主题下检查唯一启动控件及其折叠菜单、单滚动区、列表操作、长标题和焦点，无溢出或残留遮罩。（证据 5.4 / 6.3–6.5）
- [x] 5.5 验证升级与回退：原会话/项目/编码关联/账号及外观偏好保留，旧中间栏不恢复；开关关闭恢复原侧栏入口和数量，coding 团队过滤及服务端拒绝仍生效。（证据 5.5，含本次发现并修复的回退缺陷）
- [x] 5.6 运行与共享代码相关的草稿/附件、任务导航、后台回复、会话寻址及旧客户端兼容回归；如新增接口或更改授权登记，完成对应路由覆盖检查。（证据 5.6；未新增接口、未改授权登记）
- [x] 5.7 更新用户操作文档与发布说明，记录开关启用/回退步骤和「coding 不显示在团队选择界面」规则，将最终检查结果写入 evidence；运行 OpenSpec 严格校验。（证据 5.7；三语文档已更新，发布文案与开关步骤已记录，`openspec validate --strict` 通过）
- [x] 5.8 仅在上述生产前置与实际页面验收通过后启用新版侧栏；未通过时保留开关关闭并记录原因，不用占位接口或模拟数据宣布已上线。（证据 5.8：门槛已满足；本次不写运行 `config.json`，实际开启属发布动作）

## 6. 启动控件与会话历史页共用（修订）

侧栏不再放第二个启动按钮：「新建对话」与历史页「新对话」合并为同一个控件——主按钮直接开始单人对话，右侧折叠箭头打开同一份选择菜单（逐个智能体的单人对话与「多智能体对话」）。

- [x] 6.1 修订规范：`workbench-sidebar-launch` 的快速发起区改为唯一一个「新建对话」控件并规定与会话历史页共用菜单；`agent-team-conversation` 明确团队入口可用性只取决于可用智能体是否多于一位；`session-history-workbench` 明确两个入口共用同一选项与创建流程。（证据 6.1）
- [x] 6.2 实现共用控件：以 `NEW_CHAT_SURFACES` 统一描述两个入口的按钮、折叠箭头、菜单与主按钮行为，`paintNewChatMenu` / `closeNewChatMenus` / `syncNewChatControls` 供两处复用；删除第二启动按钮与 `startSidebarTeamChat`，避免两套菜单各自演化。（证据 6.2）
- [x] 6.3 布局与定位：启动块改为定位上下文并保持列向 flex（按钮仍撑满侧栏宽度，修订前首轮实现曾因丢失去掉该撑满行为并在真实页面验收中失败），折叠箭头绝对定位不挤动居中标签，展开态菜单锚定按钮下方，桌面折叠侧栏在右侧打开浮层，admin 壳隐藏整个启动块。（证据 6.3）
- [x] 6.4 交互细节：打开任一菜单会关闭另一个；手机从抽屉打开团队选择器时先收起抽屉；Escape 的焦点回到用户实际使用的启动按钮，而不是随菜单一起隐藏的菜单行。（证据 6.4）
- [x] 6.5 更新测试：`test_agent_chat_launch_frontend.cjs` 断言两个入口共用同一份菜单且侧栏折叠箭头受呈现开关约束，`test_team_chat_launch_frontend.cjs` 覆盖焦点回到启动控件与手机抽屉收起，`test_workbench_sidebar_launch_frontend.cjs` 断言单一启动控件与定位钩子，`test_sidebar_launch_browser.cjs` 在真实页面验收菜单一致性、锚定、折叠浮层与回退。（证据 6.5）
- [x] 6.6 同步用户文档（`docs/zh`、`docs/ja`、`docs/` 的多智能体协同页）与 change 产物（proposal / design / tasks / evidence）。（证据 6.6）
- [x] 6.7 收尾修正重复 id：历史页按钮改用 `history-new-chat-btn` 并让 `NEW_CHAT_SURFACES.panel.control` 指向它，避免焦点恢复被输入框的加号按钮抢走；补「面板焦点归属」与「页面 id 唯一」两条断言。（证据 6.7）
