## 实施状态（本次 apply 记录）

本次 apply 的范围是 **G0 控制面 + G1 控制面**（第 2 组、第 3 组），其余按门槛阻塞：

- 已完成并有测试证据：第 2 组全部 7 项，第 3 组 3.1–3.6。
  - 存储与服务：`tests/test_external_connection_schema.py`、`tests/test_external_connection_service.py`
  - API 与权限（含匿名、成员、只读、租户管理员、平台管理员、零租户平台管理员、越权 ID）：`tests/test_external_connections_api.py`
  - 切片与路由一致性：`tests/test_capability_matrix.py`、`tests/test_route_registry.py`、`tests/test_http_policy.py`、`tests/test_identity_policy.py`、`tests/test_effective_capabilities.py`、`tests/test_personal_capability_switches.py`
- 阻塞项及原因：
  - 1.1–1.5：G0 追踪表/迁移清单/门槛证据模板属于独立交付物，需要真实联调环境与维护人记录；本次只完成了其中的依赖切片核对（凭据能力被复用而非新建，能力投影已登记「测试/执行保持关闭」及其原因），未产出证据目录与模板。解除条件：指定 evidence 目录与真实环境登记人。
  - 3.7 / 13.x：G1 签署与发布需要外部环境验收证据，且必须以第 1 组缺口核对完成为前置；本次不可能取得，故保持未勾选。**注意：未取得测试与执行的外部验收，因此类型投影只声明读/配置类动作，`test`/`execute` 不登记为能力，路由不存在（见 `auth/capability_matrix.py` 与 `integrations/external/registry.py`）。**
  - 第 4–9 组：需要受控真实 MCP/ERP/OA/IMAP/SMTP 环境与隔离执行器验收，本部署不具备，保持关闭。
  - 第 10 组：控制台页面依赖 G1 之后的类型操作展示，本次未实现（切片 `page=None`）。
  - 第 11–12 组：依赖 Desktop/Channel/调度证据与旧数据迁移维护窗口，未开始。

## 1. G0 基线与依赖证据

- [ ] 1.1 固定实施基线版本，核对 Demo 与九份 delta spec，建立需求到代码、测试、门槛的追踪表；记录 MCP/ERP/OA/邮箱的真实、缺失与模拟能力。（阻塞：证据模板与真实环境登记待定）
- [ ] 1.2 盘点当前 MCP workspace/配置回退、ERP 文件实际租户归属、Agent 绑定、连接 ID、工具别名和旧 API 调用方，形成脱敏迁移清单。（阻塞：属第 12 组迁移前置，本次未执行存量数据盘点）
- [ ] 1.3 核对 audit-log、credential-management、resource-execution-authorization、resource-quota 的所需切片代码与验收证据；缺口登记为本 change 的前置修复任务，不以函数存在认定通过。（阻塞：仅完成 credential-management 复用核对，其余三项证据待补）
- [ ] 1.4 核对 action-approval、execution-isolation、desktop-tenant-context 的消费边界与当前证据，分别记录写操作、stdio、Desktop 保持关闭的原因及解除条件。（部分完成：已把「测试/执行保持关闭」的原因写入能力投影）
- [ ] 1.5 在实施期 evidence 目录建立门槛记录模板，登记真实联调环境、维护人、构建、测试命令、脱敏结果和客户端范围；确认共享文件的合并边界。（阻塞：evidence 目录未建立）

## 2. 连接数据与秘密管理（前置 G0）

- [x] 2.1 在身份控制库新增 external_connections 及范围/类型约束、软删除、版本、OA/邮箱/覆盖唯一索引，验证升级与重复迁移幂等。
- [x] 2.2 新增目录版本/ERP 默认记录、平台租户可用名单、测试摘要和迁移记录表，验证跨租户外键、默认唯一与删除引用约束。
- [x] 2.3 实现连接服务的创建、详情、分页目录、更新、停用和删除事务，原子提交版本及脱敏审计，禁止客户端变更 owner/tenant/kind。
- [x] 2.4 复用租户/个人凭据与版本接口，实现秘密槽 keep/replace/clear 和原子关联；覆盖空白保留、显式清除、掩码误提交及主密钥缺失。
- [x] 2.5 实现独立平台 MCP 服务秘密与版本存储及受限解析入口，验证无虚构租户、无租户/个人秘密升级、无任意平台秘密引用。
- [x] 2.6 实现 If-Match、目录版本 CAS、创建幂等键及脱敏结果核对；覆盖双窗口写入、单例竞态、默认切换竞态和响应丢失重试。
- [x] 2.7 验证秘密轮换/撤销、事务失败、异常和日志脱敏；确认配置投影、审计、URL、错误、幂等记录均无可复用秘密。

## 3. 管理 API、权限与 G1 验收（前置第 2 组）

- [x] 3.1 登记 external_connection 资源、租户/平台/本人操作权限及 capability slices，保留 ERP 旧权限的等价 ERP 范围映射，禁止扩权。（旧 ERP 接口的等价映射随第 6 组切换完成，本次只保证不扩权）
- [x] 3.2 实现 catalog/types 投影与 tenant CRUD/default/override/restore 路由；静态路由先匹配，返回服务端 actions 与真实不可用原因。
- [x] 3.3 实现 personal 邮箱与 platform MCP 路由、平台租户可用名单管理；覆盖零租户平台管理员、同用户跨租户和管理员不能代用他人邮箱。
- [x] 3.4 接入既有认证、管理写保护、对象范围及统一错误契约；移除新管理 API 对 chat.use 的意外依赖，所有管理 mutation 均验证来源。
- [x] 3.5 实现引用摘要、删除阻止/显式解绑、默认清空/替换与覆盖恢复的原子语义，防止静默改用另一连接。
- [x] 3.6 运行 API 权限矩阵与越界 ID 测试，覆盖匿名、受限账号、成员、租户管理员、平台管理员和合法机器上下文；确认管理资格不授予 tool.execute。
- [ ] 3.7 核验第 1 组所列审计/凭据/授权缺口已补齐并附证据，签署 G1；仅允许已验收类型配置，测试与执行开关继续关闭。（阻塞：1.3/1.4 缺口证据未完成，无法签署 G1）

## 4. 连接测试基础设施（前置 G1）

> 阻塞：需要可取消的测试池、网络策略与真实目标环境；本部署无可验收外部环境，测试能力保持关闭。

- [ ] 4.1 实现类型适配器 validate/probe/describe/invoke 契约与执行上下文，禁止适配器从全局环境猜租户。
- [ ] 4.2 实现有界同步测试池、分范围限流/配额、I/O 超时取消和资源释放；不可中断 SDK 调用使用可终止 worker，并验证压力下不拖垮 Web。
- [ ] 4.3 实现目标网络允许策略、DNS/重定向/认证 origin 校验、TLS 默认校验及部署例外审计；验证合法内网接入与 SSRF 负例。
- [ ] 4.4 实现草稿 test 与已保存 test 的秘密解析/版本绑定；草稿不持久化，旧版本结果不能写当前状态，保存后需重新测试。
- [ ] 4.5 实现阶段化测试结果与错误码，覆盖认证、TLS、网络、超时、依赖缺失、策略拒绝和协议部分成功；检查无副作用及无秘密回显。

## 5. MCP 适配与运行时接入（前置第 4 组）

> 阻塞：stdio 需要已验收隔离执行器（1.4），三种传输的联调需要受控真实 MCP 环境。

- [ ] 5.1 实现 stdio/SSE/Streamable HTTP 的字段 schema、秘密 headers/env 与参数校验，复用现有 MCP 握手和工具发现协议。
- [ ] 5.2 实现平台继承、租户完整覆盖、本租户停用、恢复继承及来源版本，覆盖平台模板更新、平台撤权、源停用与源删除引用冲突。（继承/覆盖/恢复的数据语义已在第 2 组实现并通过服务测试；此处待接运行时读取）
- [ ] 5.3 接入已有 OAuth 授权/回调/刷新/撤销链，绑定主体、连接、范围、一次性 state 和配置版本，验证跨租户及迟到回调拒绝。
- [ ] 5.4 将 ToolManager 配置读取与后台刷新接入范围感知的连接 resolver，保留稳定工具身份/旧别名和原 Agent 绑定，禁止名字前缀作为授权证明。
- [ ] 5.5 更新 MCP 连接/进程注册和缓存失效机制，按实际消费租户、版本和凭据隔离；验证轮换、停用、撤权及工具移除后下一次调用失败。
- [ ] 5.6 接入发现和实际派发的资源授权、Agent 范围及配额；验证租户管理员 MCP 例外不跨绑定智能体租户、不绕过执行条件。
- [ ] 5.7 将 stdio 启动接入已验收隔离执行器与允许程序/参数策略，验证宿主资源、路径和进程生命周期；隔离未通过时保留禁止启动/测试行为。
- [ ] 5.8 使用受控真实 MCP 环境完成三种传输与 OAuth 正反例联调，分别登记 MCP G2/G3 与 stdio 的独立依赖证据；模拟服务器仅作契约测试。

## 6. ERP 统一管理与场景兼容（前置第 4 组）

> 阻塞：需要 SAP RFC/ADT 真实环境（含 U9/金蝶现状确认）才能注册 ERP 类型并使配置验收。

- [ ] 6.1 实现 SAP RFC/ADT SQL 配置 schema、SDK readiness 及真实只读 probe，区分连接测试与业务调用，不执行写 BAPI。
- [ ] 6.2 实现 ERP 默认选择及无默认提示，验证首次创建、停用默认、清空默认与并发替换，禁止隐式切换。（默认指针与 CAS 已在第 2/3 组实现；此处待接场景解析）
- [ ] 6.3 将 Scene ERP options、采购同步和 SAP 分析接入统一 resolver，保留合法 connection_id、Agent/租户范围及 ExecutionRun。
- [ ] 6.4 改造旧 ERP GET/POST 为掩码/版本兼容接口及原子授权事务；旧无版本写入返回升级错误，停止 JSON 写入旁路。
- [ ] 6.5 将 Scene 管理连接入口转至正式页面，移除第二套可写表单与重复全局 handler；验证不依赖聊天权限管理连接。
- [ ] 6.6 禁止生产 U9/金蝶模拟分支作为真实 probe 或执行回退，旧模拟连接显示禁用与迁移原因，不能成为默认。
- [ ] 6.7 完成 SAP RFC/ADT 真实环境最小联调及旧场景回归，登记 ERP G2/G3、依赖缺失和认证失败证据。

## 7. OA 配置与查询（前置第 4 组）

> 阻塞：需要受控 OA 环境分别验证登录型与 OpenAPI 查询。

- [ ] 7.1 实现租户 OA 单例及登录/OpenAPI 参数 schema，覆盖秘密保留和各能力条件必填，禁止硬编码示例站点参数。
- [ ] 7.2 实现账号登录 probe 和 OpenAPI readiness 检查，发现参数仅作为待确认值返回；区分登录通过与 OpenAPI 未就绪。
- [ ] 7.3 实现受控待办/已办/我发起/抄送列表查询与分页，返回真实范围及分页限制，不以门户 widget 冒充完整列表。
- [ ] 7.4 实现流程详情、审批记录、获准关联流程与附件访问，遵循远端权限和本地对象范围；纯查询不隐式标记已读。
- [ ] 7.5 将 OA 结构化工具登记到现有工具与执行链，按需解析凭据并隔离认证会话，验证轮换/跨租户/失权后不沿用旧会话。
- [ ] 7.6 使用受控 OA 环境分别验证登录型与 OpenAPI 查询，记录 E9/E10 实际支持矩阵与 OA G2/G3；缺失接口不以模拟响应补足。

## 8. 本人邮箱配置与只读工具（前置第 4 组）

> 阻塞：需要受控 IMAP/SMTP 环境做 TLS/认证联调。

- [ ] 8.1 实现 tenant+owner 邮箱单例和 IMAP/SMTP 独立 schema、默认值、显式 TLS 模式及证书策略，兼容参考 secure 字段语义。（单例与 schema 已实现；TLS 证书策略待第 4 组）
- [ ] 8.2 实现分协议网络/TLS/认证 probe 和部分成功结果，验证只启用单协议、空白密码保留、认证错误及测试不收发正文/修改标记。
- [ ] 8.3 实现文件夹/邮件列表、搜索和正文读取，使用不改变已读状态的只读获取方式，输出处理恶意邮件 HTML 与超大内容。
- [ ] 8.4 实现附件下载与允许目录解析、最终文件访问校验及配额，覆盖路径穿越、绝对路径、符号链接和文件名冲突。
- [ ] 8.5 将邮箱工具绑定当前本人、租户与 Agent 执行上下文，验证平台管理员/同租户管理员不能代用，以及切租户、撤权、轮换后的失效。
- [ ] 8.6 使用受控 IMAP/SMTP 环境完成 TLS/STARTTLS、认证和只读联调，登记邮箱 G2/G3，明确只收件/只发件及服务端不支持场景。

## 9. 受控写动作与 G4（前置对应类型 G3）

> 阻塞：写动作必须逐动作取得真实故障联调证据；依赖三 G3 门槛，未达成，写能力保持关闭。

- [ ] 9.1 核验既有 action-approval、审计和硬配额的消费切片，补齐具体依赖缺口；建立动作风险目录，未知 MCP 工具默认高风险，不信任远端 readonly 声明。
- [ ] 9.2 将连接/秘密版本、主体/租户、目标对象、规范化参数和附件摘要绑定审批；实现派发前重校，覆盖审批人分离、过期、撤销和参数变化。
- [ ] 9.3 实现 SMTP 发送的收件人/抄送/密送/主题/正文及受控附件；实现独立授权的已读/未读标记，发送与查询权限分开。
- [ ] 9.4 实现 OA 流程创建、提交/同意与退回的受控入口，验证远端可操作状态，按 E9/E10 支持情况选择适配器。
- [ ] 9.5 实现 OA 适配器支持的转发/转办/传阅/加签及抄送已读副作用的独立动作授权；目标人歧义不自动选择，未支持接口明确关闭。
- [ ] 9.6 将 MCP 实际工具派发纳入风险、审批与配额策略；验证管理页面、原工具入口、Channel、调度不能旁路同一动作规则。
- [ ] 9.7 实现 outcome_unknown、远端核对及重复提交保护，覆盖已受理后超时、取消竞争和响应丢失，禁止无条件重发邮件/审批。
- [ ] 9.8 在受控测试对象上完成真实写入与故障联调，逐动作附 G4 证据；未验收动作继续关闭，不能用只读完成代替全 change 完成。

## 10. 正式控制台页面（前置 G1，类型操作按 G2–G4 展示）

> 未开始：本次只交付控制面 API（切片 `page=None`，页面投影不出现），页面与交互属后续批次。

- [ ] 10.1 登记「模型与接入 / 外部系统接入」菜单、稳定路由及独立懒加载模块，位于消息渠道之后，classic/split 共用同一地址与页面。
- [ ] 10.2 实现卡片、可见范围统计、搜索/类型/状态筛选和加载/空/失败/无权状态，绑定服务端 capability/actions，无 Demo 角色与数据。
- [ ] 10.3 实现新增类型选择及 MCP/ERP/OA/邮箱差异表单，覆盖字段联动、协议条件、秘密替换/清除、单例跳转和明确范围。
- [ ] 10.4 实现测试中/分阶段失败/部分成功/过期显示，保存与测试分离；使用请求 epoch 丢弃迟到结果并保留可编辑错误输入。
- [ ] 10.5 实现启停、删除、默认切换、平台覆盖/恢复和引用冲突处理，展示操作影响；处理 If-Match 冲突和响应未知，不自动覆盖。
- [ ] 10.6 接入关闭抽屉/切换对象/类型/租户/导航的离页保护与身份失效清理，确认秘密不进入 URL、localStorage、遥测和永久草稿。
- [ ] 10.7 完成简/繁/英文、既有主题、390px 窄屏、大字号、键盘、焦点返回和辅助技术验证；将截图与交互回归附入证据。
- [ ] 10.8 验证成员本人邮箱、管理员租户连接、零租户平台 MCP 三类完整路径及直接地址拒绝场景，确保菜单/标题/面包屑和错误状态一致。

## 11. 客户端、Channel 与调度边界（前置对应类型门槛）

> 阻塞：依赖 Desktop 租户上下文与对应类型 G2/G3，未开始。

- [ ] 11.1 核验 Desktop 租户上下文切片后扩展 broker 允许路由/方法及原生请求契约，renderer 不持令牌、不走绝对 URL 旁路。
- [ ] 11.2 在原生构建中完成双用户/双租户切换、旧请求返回、退出登录、凭据撤权与窗口恢复测试，形成 Desktop 独立证据；未通过不开放。
- [ ] 11.3 验证 Channel 身份到 Agent/tenant 的实际传播和 per-call 授权；用户、机器主体不混用，个人邮箱必须有明确本人委托。
- [ ] 11.4 验证 scheduler 延迟执行时的当前成员资格、委托、凭据版本、审批和配额，禁止复用入队时授权或伪造 Membership。
- [ ] 11.5 回归 Web/Desktop/Channel 的既有非外部连接工具、Agent Registry/Bridge、业务 session 与 ExecutionRun，按客户端分别记录通过或未开放范围。

## 12. 存量迁移与恢复（前置 G1，切换前核验对应执行门槛）

> 阻塞：需要维护窗口与存量盘点（1.2），未开始。

- [ ] 12.1 实现本项目旧 MCP/ERP 脱敏预检命令、源哈希和归属/标识映射，覆盖同名不同配置、workspace 绑定、回退来源及归属不明拒绝。
- [ ] 12.2 实现维护窗口锁定、加密备份、幂等导入与事务中断恢复，保留 ERP ID、MCP 工具别名/Agent 绑定/授权，模拟 ERP 禁用迁入。
- [ ] 12.3 实现范围级 store_version 切换、旧 API 转接和运行时统一读取，验证新源失败不回退 JSON，阻断旧版无条件写入和双写。
- [ ] 12.4 核对迁移前后非秘密字段、秘密版本、默认/继承关系、场景引用和授权集合，证明不扩权、不跨租户合并。
- [ ] 12.5 按运维策略移除在线旧明文秘密、处理受控备份保留、刷新进程/会话/发现缓存，并出具脱敏清理报告。
- [ ] 12.6 演练切换前中断、切换后功能回退及受控整库恢复，确认不复活已撤销凭据、不回退旧明文消费者，记录外部副作用的核对方式。

## 13. G5 发布与交付

> 阻塞：G5 以各门槛证据齐备为前置，未达成。

- [ ] 13.1 将所有需求场景对应到测试/联调证据，运行本 change 严格校验及受影响回归；修正实现期间发现的规范差异，不能只调整勾选状态。
- [ ] 13.2 验证 feature flags 默认关闭、配置/测试/只读/写操作及客户端各门槛独立，故障回退和未验收状态不会投影为可用能力。
- [ ] 13.3 完成运维配置、网络/CA/SDK、备份恢复、API 不兼容项、凭据轮换与用户操作文档，附 OneAgent 能力对照及真实 U9/金蝶未交付说明。
- [ ] 13.4 检查与现有附件作用域 change 的合并回归，确认未覆盖其导航、业务数据源和权限修复。
- [ ] 13.5 按类型、动作、客户端批准并执行实际发布，登记构建和证据；只有所有承诺交付任务完成后才归档 change，分阶段上线不等于整体完成。
